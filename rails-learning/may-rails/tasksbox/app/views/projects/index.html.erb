<br/><%= notice %>

<h2> Listing Projects : <%= @projects.count %> Projects </h2> 
<%= link_to "Register New Project", new_project_path %> | <%= link_to "Export to Excel", projects_path(format: "xls") %> <br/><br/>

<% if @projects.empty? %>
    <h3> Oops! Looks like there are No Projects registered ! :( </h3>
    <% link_to "Register Project", new_project_path %>
<% else %>
    <p> New and Ongoing Projects </p>
    <div class="list-group">
        <ol>
            <% Project.new_ongoing_projects.each { |proj| %>
                <%= link_to proj.details, project_path(proj.id), class: "list-group-item" %>
            <% } %>
        </ol>
    </div>

    <p> Completed Projects </p>
    <div class="list-group">
        <ol>
            <% Project.completed_projects.each { |proj| %>
                <%= link_to proj.details, project_path(proj.id), class: "list-group-item" %>
            <% } %>
        </ol>
    </div>

    <!--
        <table border="1">

        <thead>
        <tr>
        <th>#</th>
        <th>Name</th>
        <th>Description</th>
        <th>Start Date</th>
        <th>End Date</th>
        <th>Status</th>
        </tr>
        </thead>
        <tbody>
        <% @projects.each_with_index { |project, index| %>
            <tr>
            <td><%= index + 1 %></td>
            <td><%= project.name %></td>
            <td><%= project.description %></td>
            <td><%= project.start_date %></td>
            <td><%= project.end_date %></td>
            <td><%= project.status %></td>
            </tr>
        <% } %>
        </tbody>
        </table>
    -->
<% end %>

